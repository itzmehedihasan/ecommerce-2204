import React from 'react'

function SubHeading({text,className}) {
  return (
    <h2 className={`font-dm font-bold text-39 text-strong ${className}`}>{text}</h2>
  )
}

export default SubHeading

  //                 npm run dev
